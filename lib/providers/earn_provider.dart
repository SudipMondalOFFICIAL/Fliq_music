// earn_provider.dart — Watch Ads and Offerwall removed

import 'package:flutter/material.dart';

class EarnProvider extends ChangeNotifier {
  // Empty provider kept for compatibility — no Watch Ads or Offerwall
  EarnProvider();
}
